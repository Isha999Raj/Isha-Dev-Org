public without sharing class myproject {
    public static string color;
    public  static boolean boolval(string col){
        color = col;
        if(col == 'green'){
            return true;
        }
        else{
            return false;
        }
    }
}